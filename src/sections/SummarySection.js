import Section from "../components/Section";

const SummarySection = () => {
    return (
        <Section title="Summary">
            <span>
                Engineering leader with 6+ years building product-grade
                distributed systems and two startups. Experienced in hiring and
                growing small engineering teams, building shared platform
                components (data pipelines, recommendation/serving infra), and
                driving cross-functional alignment between product, research,
                and engineering.
            </span>
        </Section>
    );
};

export default SummarySection;
